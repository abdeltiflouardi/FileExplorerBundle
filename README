Explorer your system and server files

INSTALLATION

    git clone http://github.com/ouardisoft/FileExplorerBundle.git vendor/bundles/OS/FileExplorerBundle

Add in autolodader file

    'OS' => __DIR__.'/../vendor/bundles',

Add in routing.yml file

    OSFileExplorerBundle:
        resource: "@OSFileExplorerBundle/Resources/config/routing.yml"
        prefix:   /file-explorer

To test http://server/file-explorer


// TODO list
Connect to ftp server
Explore server FTP files
Upload files
Manage file (rename, remove, copy, move, download)
Authentification
